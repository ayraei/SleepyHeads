import java.util.ArrayList;

/** User-based object class **/
public class User {
	/* Fields. */
	/** User ID number **/
	private int user;
	
	/** History of movie ratings. **/
	private ArrayList ratingHistory;
	
	/* Methods. */
	/** Check if user already exists. **/
	
	
	/** Get user's history of movie ratings. **/
	public ArrayList getHistory() {
		return this.ratingHistory;
	}
	
	/** Add new movies to user's rating history. **/
	public void addToHistory(Movie m) {
		this.ratingHistory.add(Movie);
	}
	
	/** Get the average of the ratings from the user. **/
	public int getAvgRating() {
		return -1;
	}
	
	/* Constructor. */
	public User(int u) {
		this.user = u;
		this.ratingHistory = new ArrayList<Movie>();
	}
}
